{
  "name": "08_reversi-fantasy",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "check-encoding": "node scripts/check-encoding.js",
    "dev": "npm run check-encoding && vite",
    "build": "npm run check-encoding && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "peerjs": "^1.5.5"
  }
}
